name: inrtu-palette
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - pytorch==2.5.1
  - torchvision==0.20.1
  - torchaudio==2.5.1
  - pytorch-cuda=12.4
  - pandas
  - numpy
  - scipy
  - pip
  - pip:
      - opencv-python
      - clean-fid
      - tqdm
