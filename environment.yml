name: inrtu-palette
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python==3.10
  - pytorch-cuda=12.4
  - pandas
  - numpy
  - scipy
  - pip
  - pip:
      - opencv-python
      - clean-fid
      - tqdm
      - torch==2.6.0
      - torchvision==0.21.0
      - torchaudio==2.6.0
      - pytorch-mssim
      - kornia
      - lpips
